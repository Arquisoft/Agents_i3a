language: java
jdk:
- oraclejdk8
#after_success:
#- bash <(curl -s https://codecov.io/bash)
env:
  - MONGO_PASS=1234
  - EUREKA_URL=https://eureka-i3a.herokuapp.com/eureka/
  - DOMAIN_NAME=https://i3a-3.herokuapp.com
before_script:
- "export DISPLAY=:99.0"
- "sh -e /etc/init.d/xvfb start"
- sleep 150 # give xvfb some time to start
